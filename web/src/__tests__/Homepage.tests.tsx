import React from 'react'
import { render } from '@testing-library/react'

import HomePage from '../pages'

describe('<Homepage />', () => {
	it('should add tests later when homepage is further implemented', () => {
		render(<HomePage />)
		expect(true).toEqual(true)
	})
})
