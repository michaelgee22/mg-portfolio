import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    reddit: {
      100: '#FF4602', // orange
      200: '#1B1B1C', // gray
      300: '#030303' // black
    }
  }
})

export default theme
